/**
 * Design tokens for UI
 */


const FONT_SIZES = Object.freeze({
    small: 14,
    medium: 16,
    large: 18,
});

const SPACING = Object.freeze({
    extraSmall: 4,
    small: 5,
    medium: 10,
    large: 15,
});

const ICON_FONT_SIZES = Object.freeze({
    large: 35,
});

const SIZES = Object.freeze({
    searchBarHeight: 50,
    businessImageWidth: 220,
    businessImageHeight: 140,
});

export { FONT_SIZES, SPACING, ICON_FONT_SIZES, SIZES };


